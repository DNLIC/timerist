You are building stopwatch.html for Timerist by starting from the desired modern styled shell (use the structure and styles of quick-countdown.html or the latest interval.html as the reference) and integrating only the stopwatch specific functionality.

Base file content:

[<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stopwatch - Timerist</title>
    <link rel="stylesheet" href="styles/timerist-design-system.css">
    <style>
        /* Header with logo on left and menu on right */
        .header-top {
            position: absolute;
            top: clamp(1rem, 3vw, 2rem);
            left: 0;
            right: 0;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 clamp(1rem, 3vw, 2rem);
            z-index: 10;
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
        }
        
        .header-actions {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            flex-shrink: 0;
        }
        
        .header-top .mode-toggle-btn {
            position: relative;
            top: auto;
            right: auto;
            width: 44px;
            height: 44px;
            flex-shrink: 0;
        }
        
        #timerist-logo {
            display: block;
            width: 40px;
            height: 40px;
            color: #0a0a0a;
        }
        
        .header-top a {
            transition: opacity 0.2s ease;
        }
        
        .header-top a:hover {
            opacity: 0.7;
        }
        
        #timerist-logo circle,
        #timerist-logo line {
            stroke: #0a0a0a;
            stroke-width: 2.5;
        }
        
        .dark #timerist-logo {
            color: #fafafa;
        }
        
        .dark #timerist-logo circle,
        .dark #timerist-logo line {
            stroke: #fafafa;
        }
        
        html.dark #timerist-logo .logo-circle,
        html.dark #timerist-logo .logo-line {
            stroke: #fafafa !important;
        }
        
        .hamburger-menu {
            background: transparent;
            border: none;
            cursor: pointer;
            padding: 0.5rem;
            display: flex;
            align-items: center;
            justify-content: center;
            color: hsl(var(--foreground));
            font-size: 1.5rem;
            transition: opacity 0.2s;
            width: 44px;
            height: 44px;
            flex-shrink: 0;
        }
        
        .hamburger-menu:hover {
            opacity: 0.7;
        }
        
        .settings-drawer {
            position: fixed;
            top: 0;
            right: -100%;
            width: 100%;
            max-width: 400px;
            height: 100vh;
            background: hsl(var(--card));
            border-left: 1px solid hsl(var(--border));
            box-shadow: -2px 0 8px rgba(0, 0, 0, 0.1);
            transition: right 0.3s ease;
            z-index: 1002;
            overflow-y: auto;
        }
        
        .settings-drawer.open {
            right: 0;
        }
        
        .drawer-overlay {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.5);
            opacity: 0;
            visibility: hidden;
            transition: opacity 0.3s, visibility 0.3s;
            z-index: 999;
        }
        
        .drawer-overlay.open {
            opacity: 1;
            visibility: visible;
        }
        
        .drawer-header {
            padding: 1.5rem;
            border-bottom: 1px solid hsl(var(--border));
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .drawer-content {
            padding: 1.5rem;
        }
        
        .drawer-close {
            background: transparent;
            border: none;
            cursor: pointer;
            font-size: 1.5rem;
            color: hsl(var(--foreground));
            padding: 0.25rem;
        }
        
        .container {
            position: relative;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding-top: clamp(4rem, 8vw, 6rem);
        }
        
        .timer-container {
            max-width: 600px;
            margin: 0 auto;
        }
        
        .timer-controls {
            display: flex;
            gap: 0.75rem;
            justify-content: center;
            margin-top: 1.5rem;
            flex-wrap: wrap;
        }
        
        .timer-controls .btn {
            min-width: 80px;
            padding: 0.75rem 1.25rem;
        }
        
        @media (max-width: 390px) {
            .timer-controls {
                gap: 0.5rem;
            }
            
            .timer-controls .btn {
                min-width: 70px;
                padding: 0.625rem 1rem;
                font-size: 0.875rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header-top">
            <div style="display: flex; align-items: center; gap: 0.75rem;">
                <a href="index.html" style="display: flex; align-items: center; text-decoration: none; cursor: pointer;">
                    <svg
                        width="40"
                        height="40"
                        viewBox="0 0 40 40"
                        xmlns="http://www.w3.org/2000/svg"
                        id="timerist-logo"
                    >
                        <circle cx="20" cy="20" r="15" fill="none" stroke="#0a0a0a" stroke-width="2.5" class="logo-circle"/>
                        <line x1="20" y1="20" x2="20" y2="8" stroke="#0a0a0a" stroke-width="2.5" class="logo-line"/>
                        <line x1="20" y1="20" x2="30" y2="20" stroke="#0a0a0a" stroke-width="2.5" class="logo-line"/>
                    </svg>
                </a>
                <span style="font-size: clamp(1rem, 3vw, 1.5rem); font-weight: 600; color: hsl(var(--foreground));">timerist.com</span>
                <span class="separator" style="color: hsl(var(--muted-foreground));">|</span>
                <span style="font-size: clamp(1rem, 3vw, 1.5rem); font-weight: 600; color: hsl(var(--foreground));">Stopwatch</span>
            </div>
            
            <div class="header-actions">
                <button class="mode-toggle-btn mode-toggle" onclick="toggleTheme()" aria-label="Toggle dark mode">
                    <span id="theme-icon">🌙</span>
                </button>
                <button class="hamburger-menu" onclick="toggleDrawer()" aria-label="Open settings menu">
                    ☰
                </button>
            </div>
        </div>
        
        <div class="timer-container">
            <div class="timer-display">
                <div class="timer-time" id="display">00:00:00</div>
            </div>
            
            <div class="timer-controls">
                <button class="btn btn-primary" id="startBtn">Start</button>
                <button class="btn btn-secondary" id="pauseBtn" disabled>Pause</button>
                <button class="btn btn-destructive" id="resetBtn">Reset</button>
            </div>
        </div>
    </div>
    
    <div class="drawer-overlay" id="drawerOverlay" onclick="closeDrawer()"></div>
    <div class="settings-drawer" id="settingsDrawer">
        <div class="drawer-header">
            <h2 style="font-size: 1.25rem; font-weight: 600; color: hsl(var(--foreground));">Settings</h2>
            <button class="drawer-close" onclick="closeDrawer()">×</button>
        </div>
        <div class="drawer-content">
            <div>
                <a href="index.html" style="display: block; padding: 0.75rem; color: hsl(var(--primary)); text-decoration: none;">← Back to Timers</a>
            </div>
        </div>
    </div>
    
    <script src="scripts/dark-mode-toggle.js"></script>
    <script src="scripts/stopwatch.js"></script>
    <script>
        function toggleDrawer() {
            const drawer = document.getElementById('settingsDrawer');
            const overlay = document.getElementById('drawerOverlay');
            drawer.classList.toggle('open');
            overlay.classList.toggle('open');
        }
        
        function closeDrawer() {
            const drawer = document.getElementById('settingsDrawer');
            const overlay = document.getElementById('drawerOverlay');
            drawer.classList.remove('open');
            overlay.classList.remove('open');
        }
        
        function updateThemeIcon() {
            const icon = document.getElementById('theme-icon');
            const isDark = document.documentElement.classList.contains('dark');
            icon.textContent = isDark ? '☀️' : '🌙';
        }
        
        updateThemeIcon();
        const observer = new MutationObserver(updateThemeIcon);
        observer.observe(document.documentElement, {
            attributes: true,
            attributeFilter: ['class']
        });
        
        const originalToggle = window.toggleTheme;
        window.toggleTheme = function() {
            originalToggle();
            setTimeout(() => {
                updateThemeIcon();
                updateLogoColors();
            }, 10);
        };
        
        // Function to update logo colors based on theme
        function updateLogoColors() {
            const logo = document.getElementById('timerist-logo');
            if (!logo) return;
            
            const isDark = document.documentElement.classList.contains('dark');
            const lightColor = '#0a0a0a';
            const darkColor = '#fafafa';
            const color = isDark ? darkColor : lightColor;
            
            const circles = logo.querySelectorAll('.logo-circle');
            const lines = logo.querySelectorAll('.logo-line');
            
            circles.forEach(el => el.setAttribute('stroke', color));
            lines.forEach(el => el.setAttribute('stroke', color));
        }
        
        const themeObserver = new MutationObserver(() => {
            updateLogoColors();
        });
        themeObserver.observe(document.documentElement, {
            attributes: true,
            attributeFilter: ['class']
        });
        
        updateLogoColors();
    </script>
</body>
</html>
]


Functionality to add / preserve for [STOPWATCH.html]:

- Do NOT change any existing styles, classes, fonts, colors, button designs, hovers, layout structure, or CSS from the shell.
- Remove or hide any elements from the shell that do not fit this timer (e.g. quick-select grid for countdown-style timers, ±5s adjust buttons).
- Timer display: Keep the large centered display with MM:SS format. Add phase labels, round counters, or other status info specific to [TIMER_NAME] (e.g., "WORK"/"REST" for intervals, round number, AMRAP score, etc.). Apply color changes (normal primary color, warning yellow, danger red near end) if already present in the shell logic.
- Controls: Use the shell's thumb-friendly bottom button pattern:
  - Primary blue button: Start / Resume / Pause (toggle text accordingly)
  - Destructive red button: Reset
- Setup fields: Place timer-specific input fields (e.g., work/rest time, rounds, duration, etc.) in the main area for quick access, using grid layout similar to interval.html. Mirror the same fields in the "Setup Content" section of the drawer.
- Hamburger menu / Drawer: Keep the shell’s drawer structure. In "Setup Content" include the setup fields + a short "About This Timer" paragraph describing typical use cases for [TIMER_NAME] and relevant SEO keywords (e.g. "Tabata timer, HIIT Tabata, 20/10 interval timer"). Keep "General Info" with Version v0.1 and release date.
- JavaScript: Preserve and adapt the [TIMER_NAME]-specific vanilla JS logic (phase switching, round tracking, counting up/down, pause/resume/reset, end beeps/visual cues). Integrate it into the shell’s JS structure. Use simple beep sounds via AudioContext if needed.
- Preserve all shell features: System fonts (-apple-system etc.), RGB CSS variables, simple hover states (opacity or subtle bg change only), Tailwind utility classes, dark/light mode via class toggle, hamburger menu, theme switcher with moon/sun icon, mobile-responsive layout.

Important rules to follow strictly:

- **Dark/Light mode only**: No references to multiple styles (style1, style2, style3) or URL parameters. Use Tailwind's dark: variants and document.documentElement.classList.toggle('dark').
- **Button hovers**: Ensure hover states maintain visibility and contrast (e.g. hover:bg-primary/90). Never let buttons disappear on hover (no white-on-white or black-on-black).
- **Top spacing**: Minimize vertical whitespace above the timer container. Pull the timer display and controls close under the header (use small pt- or mt- values like pt-2 or clamp for mobile). Optimize for portrait smartphone view so the timer is near the top with little wasted space.
- **Adherence**: Use only ShadCN-inspired components and Tailwind utilities. No custom non-Tailwind CSS selectors, external libraries, or inline styles that override the shell. Match typography, spacing, shadows, and border-radius exactly from the shell.
- **Mobile optimization**: Test layout for small screens (320–480px width). Keep header compact, hide separator/text if needed, ensure buttons are tappable.
- **Do NOT add**: Inter font, HSL variables, hover transforms/scales/extra shadows, min-height extras, or any elements specific to other timers (e.g. quick-select presets unless required for this timer).

Output the complete updated stopwatch.html code.